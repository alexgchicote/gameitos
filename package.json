{
  "name": "gameitos",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "turbo run db:generate --filter=@gameitos/db",
    "db:push": "turbo run db:push --filter=@gameitos/db",
    "db:studio": "turbo run db:studio --filter=@gameitos/db"
  },
  "devDependencies": {
    "@turbo/gen": "^2.5.4",
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "prettier": "^3.6.2",
    "turbo": "^2.5.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@9.12.0",
  "dependencies": {
    "@supabase/ssr": "^0.6.1"
  }
}